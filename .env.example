DB_HOST=localhost
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=rust_learning

# sqlx-cli only reads this form (sqlx migrate add/run/revert) — keep in sync with DB_* above
DATABASE_URL=postgres://user:password@localhost:5432/rust_learning
